/* this is just "test3" from the makefile */
/* Hunter Leath -- put your name in a comment so that if this file
   gets mixed up I can still give you credit for it */

y := 0;
while y == 0 do {
  x := 15;
  { let z = 10 in
    { let x = 5 in
        x := x + 1;
        y := 1 };
    x := x * 40 };
  print x
};
print y
